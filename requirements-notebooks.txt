# Dependencies for the HFF demonstration notebooks (symbolic regression /
# classification). The hff core itself is built from source with maturin; see
# the README. Install these with:  pip install -r requirements-notebooks.txt
#
# Version floors reflect a known-good environment; newer patch releases are
# expected to work. For the equation-recovery notebook's dataset loaders, also
# install requirements-datasets.txt.

numpy>=1.23
pandas>=1.5
scipy>=1.10
sympy>=1.12
scikit-learn>=1.2
matplotlib>=3.6
seaborn>=0.12
graphviz>=0.20
geppy>=0.1.3
deap>=1.4
multiprocess>=0.70
numexpr>=2.8
numba>=0.58

# Optional enhancement, not on PyPI — the notebooks run without it and skip the
# egglog-backed snap / physics-prior mutations if it is absent:
#   gamakAST  (github.com/... ; install separately or leave uninstalled)
