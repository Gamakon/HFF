# Dependencies for the pymoo benchmark harness.
#   pip install -r benchmark/requirements.txt
#
# IMPORTANT: pymoo 0.6.x calls np.row_stack, which numpy removed in 2.0. The
# NSGA-II/III baselines fail on numpy>=2 with
#   "module 'numpy' has no attribute 'row_stack'"
# (the HFF runs are unaffected). Pin numpy<2 until pymoo is upgraded.

# Core
numpy<2.0
pymoo>=0.6,<0.7
pandas>=1.5
pyarrow>=12

# Analysis / figures
matplotlib>=3.6
seaborn>=0.12

# Dev / testing (optional)
pytest>=7.0
pytest-cov>=4.0

# The hff core is built from source with maturin (see top-level README).
#
# GNBG benchmarks additionally require the gnbg_gpu wgpu crate, which is NOT on
# PyPI. Install it from github.com/Gamakon/GNBG-II, or set GNBG_GPU_PATH to
# its python/ directory. WFG/DTLZ benchmarks need none of this.
